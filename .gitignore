# The keys (one COBRAIN_KEY_<WORKSPACE> per workspace) are credentials, not source.
# They live in settings.local.json; the committed shape is settings.example.json.
# settings.json is the shared half (COBRAIN_URL + permissions) and is committed.
.claude/settings.local.json
.env
.env.*
*.bak

.DS_Store
